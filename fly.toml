app = "brandboda"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile"

# ---------------------------------------------------
# 🔥 الجزء الجديد: ربط البوت بالبورت ومنع الإيقاف
# ---------------------------------------------------
[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]

[processes]
  app = "python3 -m BrandrdXMusic"

# ---------------------------------------------------
# 🚀 إعدادات السيرفر القوية (زي ما هي)
# ---------------------------------------------------
[[vm]]
  cpu_kind = "shared"
  cpus = 2
  memory_mb = 1024

[[swap]]
  size_mb = 512
